!!!
%html
  %head
    %meta{:name => "viewport", :content => "width=device-width, initial-scale=1.0"}
    %title= content_for?(:title) ? yield(:title) : 'SmartStatistic'
    %meta{:name => "description", :content => "#{content_for?(:description) ? yield(:description) : 'SmartStatistic'}"}
    = stylesheet_link_tag 'application', media: 'all'
    = csrf_meta_tags
  %body
    %header
      = render 'layouts/navigation'
      %hr/
    %main{:role => "main"}
      = render 'layouts/messages'
      = yield
    %footer.text-xs-center
      SMARTSTATISTIC 2016
    = javascript_include_tag 'application'